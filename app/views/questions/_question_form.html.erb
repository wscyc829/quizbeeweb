<%= form_for :pq, url: post_question_path, remote: true do |q| %>
	<%= q.text_field :body, placeholder: "Type in your question here and press Enter to send", class: "post-question-tfield" %>
	<!-- <%= q.submit "Post question" %> -->
<% end %>